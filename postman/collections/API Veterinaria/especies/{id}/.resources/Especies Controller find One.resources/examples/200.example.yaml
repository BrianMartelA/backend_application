$kind: http-example
request:
  url: "{{baseUrl}}/especies/:id"
  method: GET
  headers:
    Accept: application/json
  pathVariables:
    id: string
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: "{}"
order: 1000
