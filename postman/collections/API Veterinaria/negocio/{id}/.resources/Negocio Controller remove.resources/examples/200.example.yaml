$kind: http-example
request:
  url: "{{baseUrl}}/negocio/:id"
  method: DELETE
  headers:
    Accept: application/json
  pathVariables:
    id: string
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: string
order: 1000
